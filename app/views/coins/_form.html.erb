<%= form_with(model: coin, local: true) do |form| %>
  <% if coin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coin.errors.count, "error") %> prohibited this coin from being saved:</h2>

      <ul>
      <% coin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label t('description') %>
    <%= form.text_field t('description'), class:"form-control", placeholder:"Bitcoin" %>
  </div>

  <div class="form-group">
    <%= form.label t('acronym') %>
    <%= form.text_field t('acronym'), class:"form-control", placeholder:"BTC"%>
  </div>

  <div class="form-group">
    <%= form.label :url_image %>
    <%= form.text_field t('url_image'), class:"form-control", placeholder:"http://img.com/bitcoin" %>
  </div>

  <div class="form-group">
    <%= form.label :mining_type %>
    <%= form.select("mining_type_id", @mining_type_options, {include_blank: 'Selecione...'}, class:"form-control")%>
    <%=  %>
  </div>

  <div class="actions">
    <%= form.submit nil, class: "btn btn-info" %>
  </div>
<% end %>

</br>
